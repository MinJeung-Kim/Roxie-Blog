import { IoShareSocialOutline } from "react-icons/io5";

type Props = {
  className?: string;
};
export default function ShareSocialIcon({ className }: Props) {
  return <IoShareSocialOutline className={className} />;
}
